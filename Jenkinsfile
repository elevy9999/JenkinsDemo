pipeline {
  agent any
  stages {
    stage('build') {
      steps {
        sh 'echo "hello"'
      }
    }
   stage('Test') {
            steps {
                sh 'node --version'
            }
        }
      
  }
}
